Phase 1: The Spatial Risk Engine (Python & Computer Vision)This phase transforms raw YOLO detections into actionable intelligence without requiring new infrastructure.[ ] Define static geometric zones (polygons) and tripwires (lines) within the camera's field of view (e.g., in your config.py).[ ] Implement a point-in-polygon algorithm (like cv2.pointPolygonTest) in the main worker loop to check if a tracking centroid intersects with a restricted zone.[ ] Build a state dictionary to map track_ids to timestamps to measure how long an object remains stationary or within a zone (loitering detection).[ ] Calculate vector changes between frame $t-1$ and frame $t$ to determine directional line crossing (e.g., entering vs. exiting).[ ] Modify the Redis publisher to emit structured event payloads (e.g., {"type": "alert", "severity": "WARNING", "message": "Loitering in Zone A"}) alongside the standard bounding box data.Phase 2: Video Transport & Concurrency (Infrastructure)This phase eliminates MJPEG bottlenecks to achieve true sub-second latency and smooth UI rendering.[ ] Add a lightweight streaming server like MediaMTX or go2rtc to your docker-compose.yml.[ ] Modify inference_worker/worker.py to write processed frames to the streaming server via an RTSP endpoint (using cv2.VideoWriter with a GStreamer backend) instead of pushing raw bytes to Redis.[ ] Remove the MJPEG StreamingResponse endpoint from the FastAPI gateway.[ ] Update the React <VideoPlayer /> component to natively consume the WebRTC stream from the media server.Phase 3: The Command Center UI (React & FastAPI)This phase brings the "UrbanAI" dashboard aesthetic to life using the robust event-driven data you are now generating.[ ] Build a CameraGrid.jsx component that maps over active session_ids to render multiple video feeds and WebSockets simultaneously.[ ] Create a background task in gateway/main.py using psutil and pynvml to query live CPU, RAM, and GPU statistics.[ ] Broadcast these hardware metrics to a dedicated Redis channel (e.g., telemetry:system) at 1-second intervals.[ ] Build a SystemTelemetryStrip.jsx component to subscribe to the telemetry channel and animate the resource usage bars.[ ] Update the AlertFeed.jsx to parse and beautifully format the new "CRITICAL" and "WARNING" alerts generated by the Risk Engine.

